spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hirewheels
    username: root
    password: Spider38@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  application:
    name: hirewheels-api

    jwt:
      secret: secret_key_for_jwt_token
      expiration: 86400000  # 24 hours